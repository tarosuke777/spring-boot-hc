spring:
  cloud:
    aws:
      dynamodb:
        enabled: true
        region: eu-west-1
        endpoint: http://localhost:18000
      credentials:
        secret-key: DUMMYEXAMPLEKEY
        access-key: DUMMYIDEXAMPLE
